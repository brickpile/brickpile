
@model Dashboard.Web.Mvc.ViewModels.IDashboardViewModel<Stormbreaker.Models.IDocument>
@using Stormbreaker.Web.Mvc.Html;

@(new MvcHtmlString(Html.SubMenu<Stormbreaker.Models.IDocument>(Model.CurrentModel,
    Model.StructureInfo,
    page => Html.ActionLink(page.Name, "edit", new { model = page }),
    page => Html.ActionLink(page.Name, "edit", new { model = page }, new { @class = "selected" }))))