
@model String
@{
    var parentModel = ViewData["ParentModel"] as BrickPile.Core.IPage;
}           
@if (parentModel == null) {
    @Html.TextBoxFor(_ => Model, new { disabled = "disabled" })
} else {
    @Html.TextBoxFor(_ => Model, new { @class = "slug" })
}
