define([
        'jquery',
        'underscore',
        'backbone'
    ], function($, _, Backbone) {

        var ContentType = Backbone.Model.extend({
            
        });
        
        return ContentType;
    });