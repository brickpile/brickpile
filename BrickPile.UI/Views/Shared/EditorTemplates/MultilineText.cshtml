@using BrickPile.Samples.Models
@model string

<textarea name="@ViewData.TemplateInfo.GetFullHtmlFieldName(string.Empty).ToCamelCase()" cols="60" rows="5">{{@ViewData.TemplateInfo.GetFullHtmlFieldName(string.Empty).ToCamelCase()}}</textarea>